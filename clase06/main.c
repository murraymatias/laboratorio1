#include <stdio.h>
#include <stdlib.h>
#include <limits.h>
#include "utn.h"
#define MAX 10
#define ARRAYSIZE(x)  (sizeof(x) / sizeof((x)[0]))


int main()
{
/*
    int search;
    float promedio;
    int array[MAX];
    utn_getArrayInt(array,MAX,"Ingrese un numero: ","\nError.",INT_MIN,INT_MAX,10);
    calcularPromedio(&promedio,0,MAX,array);
    printf("\nEl promedio es: %.2f",promedio);
    bubbleSortInt(array,MAX);
    printf("\nEl minimo es %d",array[0]);
    printf("\nEl maximo es %d",array[MAX-1]);
    utn_printArrayInt(array,MAX);
    printf("\nIngrese numero a buscar: ");
    scanf("%d",&search);
    if(utn_isInArrayInt(array,MAX,search))
    {
        printf("\nSe encontro el numero en el array");
    }
    else
    {
    $45.000
Precio único y mensual
Movimientos ilimitados por caja y Red Banelco
Ver detalle
Pedila ahora    printf("\nNo se encontro el numero en el array");
    }
*/

    return 0;
}
